<%= form_with(model: tipo_propiedad, local: true) do |form| %>
  <% if tipo_propiedad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_propiedad.errors.count, "error") %> prohibited this tipo_propiedad from being saved:</h2>

      <ul>
      <% tipo_propiedad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :tipo_propiedad_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
